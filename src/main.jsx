import React from 'react';
import ReactDOM from 'react-dom/client';
import './App.css'; // This is essential!
import App from './App';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
